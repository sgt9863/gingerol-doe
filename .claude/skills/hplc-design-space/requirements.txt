# 中核ロジック（scripts/01〜04）— Python in Excel でも使う既定ライブラリ
numpy
scipy
statsmodels
pandas
pyyaml
openpyxl          # Excel 雛形（.xlsx）の読み書き

# 可視化・フロントエンド（scripts/05 と app.py 専用。Excel 側では不要）
plotly
matplotlib        # 壁の等高線の座標抽出に使用（描画は Agg、画面なし）
streamlit         # Web アプリの入口（streamlit run app.py）

# 静止画 PNG / 回転 GIF 書き出し（任意。fig.write_image・回転GIFを使う場合のみ）
# kaleido          # 静止画レンダラ（要 Chrome。plotly_get_chrome で導入）
pillow            # 回転 GIF の連結（静止画フレーム → GIF）
